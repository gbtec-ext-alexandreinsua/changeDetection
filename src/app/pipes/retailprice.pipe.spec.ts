import { RetailpricePipe } from './retailprice.pipe';

describe('RetailpricePipe', () => {
  it('create an instance', () => {
    const pipe = new RetailpricePipe();
    expect(pipe).toBeTruthy();
  });
});
